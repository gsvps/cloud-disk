{
  "name": "cloud-disk",
  "version": "1.0.0",
  "description": "基于 Cloudflare Workers 的轻量级个人网盘，D1 + R2 + KV",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build:css",
    "build:css": "tailwindcss -i ./src/styles/input.css -o ./public/styles.css --minify",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "deploy": "npm run db:migrate && wrangler deploy",
    "deploy:cloudflare": "npm run build && npm run deploy",
    "typecheck": "tsc --noEmit"
  },
  "cloudflare": {
    "products": ["Workers", "D1", "R2"],
    "bindings": {
      "DB": {
        "description": "D1 数据库，存储用户与文件元数据。默认名称：`cloud-disk`"
      },
      "KV": {
        "description": "登录会话 KV。**无需手动创建或填写 id**——留空即可，由 `wrangler deploy` 自动创建（命名通常为 `{Worker名}-kv`）。一键部署 Configure resources 步骤**不要**再单独新建 KV，以免重复。"
      },
      "R2": {
        "description": "R2 存储桶，存储上传文件。默认名称：`cloud-disk-files`"
      },
      "APP_NAME": {
        "description": "应用显示名称（可选）。**留空**则界面默认显示 `CloudDisk`；若连接已有 Git 仓库，可与 Worker 名称保持一致"
      }
    }
  },
  "dependencies": {
    "drizzle-orm": "^0.44.2",
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "drizzle-kit": "^0.31.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
