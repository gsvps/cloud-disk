{
  "name": "cloud-disk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:css",
    "build:css": "tailwindcss -i ./src/styles/input.css -o ./public/styles.css --minify",
    "db:migrate": "wrangler d1 migrations apply DB --remote",
    "deploy": "npm run db:migrate && wrangler deploy",
    "deploy:cloudflare": "npm run build && npm run deploy",
    "typecheck": "tsc --noEmit"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "D1 数据库，存储用户与文件元数据。默认名称：`cloud-disk-db`"
      },
      "KV": {
        "description": "KV 命名空间，存储登录会话。默认名称：`cloud-disk-KV`（与 R2 不可同名）"
      },
      "R2": {
        "description": "R2 存储桶，存储上传文件。默认名称：`cloud-disk-files`"
      }
    }
  },
  "dependencies": {
    "drizzle-orm": "^0.44.2",
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "drizzle-kit": "^0.31.1",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.8.3",
    "wrangler": "^4.20.0"
  }
}
